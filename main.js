function playAnimation() {}

function calcValues(values, currentYOffset) {
  let rv;

  const scrollHeight = document.querySelector(".container").scrollHeight;
  const scrollRatio = currentYOffset / scrollHeight;

  if (values.length === 3) {
  }
}
